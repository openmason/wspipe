/**
 * wspipe - main file
 * copyright (c) 2015 openmason.
 * MIT Licensed.
 */

// JSON RPC library entry point
var WebSocketPipe = function(module, debug) {
  this.debug = debug || false;
};

module.exports = WebSocketPipe;

// -- EOF
